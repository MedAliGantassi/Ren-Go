# Ren&Go – Full Business Logic & Technical Specification (Detailed)

## 1. Project Overview

Ren&Go is a web-based platform designed to connect three main actors:

* Clients (users who want to rent houses)
* Fournisseurs (property owners)
* Administrators (platform managers)

The platform allows clients to browse properties, make reservations, pay for them, leave reviews, and manage their favorites. Fournisseurs can manage their properties and receive reservations. Administrators oversee the entire system.

---

## 2. Technical Architecture

### Frontend

* ReactJS
* Handles UI/UX, forms, dashboards, and user interactions

### Backend

* Node.js with Express
* REST API architecture
* Handles business logic, authentication, and data processing

### Database

* MongoDB (NoSQL)
* Stores users, properties, reservations, payments, reviews, etc.

---

## 3. User System (Role-Based)

All users are stored in a single collection with role differentiation:

### Roles:

* CLIENT
* FOURNISSEUR
* ADMIN

### Common Attributes:

* id
* name
* email
* password (hashed)
* role
* dateInscription
* isActive (for account disabling)

### Logic:

* Authentication is required for most actions
* Authorization is role-based (middleware checks role)
* Admin can manage all users
* No physical deletion → use "deactivate account"

---

## 4. Property (Maison) Management

### Created by:

* Fournisseur only

### Attributes:

* id
* title
* description
* price
* location
* dateCreation
* owner (Fournisseur)

### Business Logic:

* A fournisseur can create multiple properties
* A property can be updated or deleted by:

  * Its owner (Fournisseur)
  * Admin
* A property is NOT globally unavailable → availability depends on reservations

---

## 5. Availability System (Calendar-Based)

### Important Change:

Availability is NOT a boolean.

### Structure:

A property has multiple availability periods defined by:

* dateDebut
* dateFin

### Logic:

* Availability is computed dynamically based on:

  * Existing reservations
  * Dates requested by client
* Multiple reservations allowed if dates do NOT overlap

### Example:

* Reservation A: 1–5 Jan
* Reservation B: 10–15 Jan
  ✔ Both valid

---

## 6. Reservation System (Core Logic)

### Created by:

* Client only

### Attributes:

* id
* dateDebut
* dateFin
* dateReservation
* status:

  * EN_ATTENTE (pending)
  * CONFIRMEE (confirmed)
  * ANNULEE (cancelled)

### Flow:

#### Step 1: Create Reservation

* Client selects property + dates
* Backend checks:

  * If dates are available
* If yes:

  * Reservation is created with status = EN_ATTENTE

---

### Step 2: Payment Window (72 hours)

* Client has up to 72 hours to pay
* During this time:

  * Reservation remains pending

---

### Step 3: Payment

* If payment succeeds:
  → status becomes CONFIRMEE

* If payment fails:
  → status remains EN_ATTENTE

---

### Step 4: Automatic Cancellation

If:

* Reservation is still pending
* AND 72 hours have passed

Then:
→ System automatically cancels it

---

### Step 5: Manual Cancellation (Client)

Client can cancel ONLY if:

* Less than 72 hours since reservation

Else:
→ Cancellation is refused

---

## 7. Payment System

### Type:

* Simulated payment (no real banking)

### Attributes:

* id
* amount
* date
* status (SUCCESS / FAILED)

### Logic:

* One reservation → 0 or 1 payment
* Payment occurs AFTER reservation
* On success:
  → Reservation confirmed
* On failure:
  → Reservation remains pending

---

## 8. Review System (Avis)

### Purpose:

* Allow rating and feedback after experience

### Types:

1. Client → rates property
2. Fournisseur → rates client

### Attributes:

* id
* note (1–5)
* commentaire
* date

### Rules:

* Only after reservation is completed
* One review per reservation (recommended)

### Advanced Logic:

* If a client gets very low ratings repeatedly:
  → Admin can suspend or deactivate account

---

## 9. Wishlist System (Favorites)

### Purpose:

* Client saves favorite properties

### Structure:

* Wishlist linked to one client
* Contains multiple properties

### Logic:

* Add property to wishlist
* Remove property from wishlist
* No duplicates

---

## 10. Admin System

### Capabilities:

* Manage users:

  * View all users
  * Deactivate accounts
* Manage properties:

  * Delete any property
* Monitor reservations
* Moderate reviews

---

## 11. Automatic System (Timer)

### Function:

Runs periodically (e.g., every hour)

### Responsibilities:

* Check all pending reservations
* If older than 72h:
  → Cancel automatically

### Implementation Example:

* setInterval() in backend
* Or cron job

---

## 12. Security & Middleware

### Authentication:

* JWT (JSON Web Token)

### Middleware:

* authMiddleware → verifies token
* roleMiddleware → checks role access

---

## 13. API Design (High-Level)

### Auth

* POST /auth/register
* POST /auth/login

### Properties

* POST /properties
* GET /properties
* PUT /properties/:id
* DELETE /properties/:id

### Reservations

* POST /reservations
* GET /reservations
* DELETE /reservations/:id

### Payment

* POST /payments

### Reviews

* POST /reviews
* GET /reviews

### Wishlist

* POST /wishlist
* DELETE /wishlist/:id

---

## 14. Key Business Rules Summary

* Reservation must be created before payment
* Payment deadline = 72 hours
* Auto cancellation after deadline
* Availability based on dates (not boolean)
* Multiple reservations allowed if no overlap
* Reviews only after experience
* Admin has full control
* Users are role-based
* Wishlist is client-only

---

## 15. Development Strategy

### Phase 1:

Authentication & Users

### Phase 2:

Property Management

### Phase 3:

Reservation Logic

### Phase 4:

Payment Simulation

### Phase 5:

Auto Cancellation System

### Phase 6:

Reviews & Wishlist

---

## 16. Conclusion

This system models a realistic rental platform with:

* Strong business logic
* Clear separation of roles
* Automated processes
* Scalable architecture

It is fully suitable for a PFE project and demonstrates:

* Backend architecture skills
* UML understanding
* Real-world system design


I am currently starting the development phase of my PFE project "Ren&Go" (house rental platform), and I want to begin with the Property (Maison) module. Before generating code, please carefully understand the following updated business logic and constraints. 🧠 Core Business Logic Update (IMPORTANT) ⦁ Property availability is NOT stored as a boolean for booking logic. ⦁ Instead, availability is calculated dynamically based on reservations. ✔ Correct Logic: A property can have multiple reservations as long as their dates do NOT overlap. ❌ Incorrect Logic: Do NOT rely on a simple "disponible: true/false" to determine booking availability. ✔ Rule: To check availability: Two date ranges overlap if: If there is any overlapping reservation with status: ⦁ "EN_ATTENTE" or "CONFIRMEE" → the property is NOT available for those dates. 🏠 Property (Maison) Model Attributes: ⦁ id ⦁ titre ⦁ description ⦁ prix ⦁ localisation ⦁ images (array) ⦁ owner (Fournisseur) ⦁ isActive (boolean → only for visibility, NOT booking) 🔐 Access Rules ⦁ Only Fournisseur can create properties ⦁ Fournisseur can update/delete ONLY his own properties ⦁ Admin can delete any property ⦁ Client can only view properties 🎯 What I Need From You Please generate: 1. Mongoose Model (Property Schema) ⦁ Clean structure ⦁ Proper types ⦁ Reference to User (owner) 2. Controller Functions ⦁ createProperty() ⦁ getAllProperties() ⦁ getPropertyById() ⦁ updateProperty() ⦁ deleteProperty() Include: ⦁ Validation ⦁ Ownership check ⦁ Error handling 3. Routes (Express) ⦁ POST /properties ⦁ GET /properties ⦁ GET /properties/:id ⦁ PUT /properties/:id ⦁ DELETE /properties/:id 4. Middleware Integration ⦁ authMiddleware ⦁ roleMiddleware (FOURNISSEUR / ADMIN) ⚠️ Important Instructions ⦁ Follow clean architecture (controller / model / route separation) ⦁ Use async/await ⦁ Use try/catch ⦁ Keep code simple but professional ⦁ Add comments for clarity 🚀 Goal I want production-ready code for the Property module so I can integrate it later with reservation logic. Thank you.

====================================================================
=== POSTMAN TEST DATA - COPY FROM HERE ===========================
====================================================================

===== 1. REGISTER USER AS PROPRIETAIRE =====
POST http://localhost:5000/api/auth/register
Body → raw → JSON:

{
  "name": "Mohamed Proprietaire",
  "email": "mohamed@proprietaire.com",
  "password": "123456",
  "role": "PROPRIETAIRE"
}

Response: Copy the "token" from response


===== 2. REGISTER USER AS CLIENT =====
POST http://localhost:5000/api/auth/register
Body → raw → JSON:

{
  "name": "Ali Client",
  "email": "ali@client.com",
  "password": "123456",
  "role": "CLIENT"
}

Response: Copy the "token" from response


===== 3. CREATE PROPERTY (Use PROPRIETAIRE Token) =====
POST http://localhost:5000/api/properties
Headers:
  Authorization: Bearer YOUR_PROPRIETAIRE_TOKEN_HERE

Body → raw → JSON:

{
  "titre": "Villa Luxueuse au Bord de Mer",
  "description": "Magnifique villa avec vue sur la mer, 4 chambres, piscine privée, jardin tropical. Idéal pour des vacances en famille.",
  "prix": 250,
  "localisation": "Hammamet, Tunisie",
  "images": [
    "https://example.com/villa1.jpg",
    "https://example.com/villa2.jpg",
    "https://example.com/villa3.jpg"
  ]
}

Response: Copy the property "id" from response


===== 4. CREATE ANOTHER PROPERTY =====
POST http://localhost:5000/api/properties
Headers:
  Authorization: Bearer YOUR_PROPRIETAIRE_TOKEN_HERE

Body → raw → JSON:

{
  "titre": "Appartement Moderne Centre Ville",
  "description": "Appartement 2 chambres au cœur de la ville, proche des commerces et restaurants. Parking inclus.",
  "prix": 120,
  "localisation": "Sousse, Tunisie",
  "images": [
    "https://example.com/appart1.jpg",
    "https://example.com/appart2.jpg"
  ]
}


===== 5. GET ALL PROPERTIES (No Token Required) =====
GET http://localhost:5000/api/properties

No Body needed


===== 6. GET PROPERTY BY ID (No Token Required) =====
GET http://localhost:5000/api/properties/YOUR_PROPERTY_ID_HERE

No Body needed


===== 7. UPDATE PROPERTY (Use PROPRIETAIRE Token) =====
PUT http://localhost:5000/api/properties/YOUR_PROPERTY_ID_HERE
Headers:
  Authorization: Bearer YOUR_PROPRIETAIRE_TOKEN_HERE

Body → raw → JSON:

{
  "prix": 280,
  "description": "Magnifique villa avec vue sur la mer, 4 chambres, piscine privée, jardin tropical. Prix mis à jour pour haute saison."
}


===== 8. CREATE RESERVATION (Use CLIENT Token) =====
POST http://localhost:5000/api/reservations
Headers:
  Authorization: Bearer YOUR_CLIENT_TOKEN_HERE

Body → raw → JSON:

{
  "property": "YOUR_PROPERTY_ID_HERE",
  "dateDebut": "2026-05-15",
  "dateFin": "2026-05-22",
  "guests": 4
}

Response: Should show totalPrice calculated automatically


===== 9. CREATE OVERLAPPING RESERVATION (Should FAIL) =====
POST http://localhost:5000/api/reservations
Headers:
  Authorization: Bearer YOUR_CLIENT_TOKEN_HERE

Body → raw → JSON:

{
  "property": "YOUR_PROPERTY_ID_HERE",
  "dateDebut": "2026-05-18",
  "dateFin": "2026-05-25",
  "guests": 2
}

Response: Should return 409 error (overlap detected)


===== 10. CREATE NON-OVERLAPPING RESERVATION (Should SUCCESS) =====
POST http://localhost:5000/api/reservations
Headers:
  Authorization: Bearer YOUR_CLIENT_TOKEN_HERE

Body → raw → JSON:

{
  "property": "YOUR_PROPERTY_ID_HERE",
  "dateDebut": "2026-06-01",
  "dateFin": "2026-06-07",
  "guests": 3
}

Response: Should work (no overlap)


===== 11. GET MY RESERVATIONS (Use CLIENT Token) =====
GET http://localhost:5000/api/reservations/my
Headers:
  Authorization: Bearer YOUR_CLIENT_TOKEN_HERE

No Body needed


===== 12. CHECK AVAILABILITY (No Token Required) =====
GET http://localhost:5000/api/reservations/check-availability?property=YOUR_PROPERTY_ID_HERE&dateDebut=2026-07-01&dateFin=2026-07-08

No Body needed


===== 13. CANCEL RESERVATION (Use CLIENT Token) =====
DELETE http://localhost:5000/api/reservations/YOUR_RESERVATION_ID_HERE
Headers:
  Authorization: Bearer YOUR_CLIENT_TOKEN_HERE

No Body needed


===== 14. DELETE PROPERTY (Use PROPRIETAIRE Token) =====
DELETE http://localhost:5000/api/properties/YOUR_PROPERTY_ID_HERE
Headers:
  Authorization: Bearer YOUR_PROPRIETAIRE_TOKEN_HERE

No Body needed


====================================================================
=== TESTING WORKFLOW ========================================
====================================================================

1. Register PROPRIETAIRE → Save token
2. Register CLIENT → Save token
3. Create 2-3 properties using PROPRIETAIRE token → Save property IDs
4. Get all properties (verify they appear)
5. Create reservation using CLIENT token + property ID
6. Try overlapping reservation (should fail with 409)
7. Create non-overlapping reservation (should work)
8. Get my reservations (verify they appear)
9. Check availability for specific dates
10. Cancel a reservation
11. Update property price
12. Delete property

====================================================================